---
'@open-slide/core': minor
---

Add a Google Slides-optimized editable PPTX export with compatible font handling and raster fallbacks for unsupported CSS regions.

Complex visualizations can opt into exact atomic image capture with the `data-pptx-raster` attribute.
